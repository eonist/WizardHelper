// swift-tools-version:5.2
import PackageDescription

let package = Package(
    name: "StorageHelper",
    products: [
        .library(
            name: "StorageHelper",
            targets: ["StorageHelper"])
    ],
    dependencies: [
    ],
    targets: [
       .target(
            name: "StorageHelper",
            dependencies: [])
    ]
)
